# urllib package — bundles parse/quote helpers used by Jinja2's
# url_quote filter and Werkzeug's URL handling.  Only the
# ``parse`` submodule is provided; ``request`` / ``error`` /
# ``response`` aren't needed for the Flask hello-world path.
