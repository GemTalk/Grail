# urllib package — ``parse`` (quote/urlsplit helpers used by Jinja2 /
# Werkzeug), ``request`` (minimal urlopen over Grail's http.client),
# and ``error`` (URLError / HTTPError).  ``response`` and the
# opener/handler framework aren't provided.
