# Manifest of CPython 3.14.4 regression-test modules run by
# scripts/run_cpython_suite.sh.  One dotted module name per line.
# Blank lines and lines starting with '#' are ignored.
#
# These are vendored under src/python/stdlib/test/ (see licenses/CPYTHON.md)
# and scored one topaz session per module.  This is a CURATED STARTER SET
# chosen from areas Grail already covers -- NOT the full ~480-module
# suite.  Grow it as test.support and the stdlib shims fill in; the
# scoreboard's `detail` column names whatever symbol is still missing.

# --- Phase 1: most likely to at least import with the trimmed support ---
test.test_textwrap
test.test_math
test.test_int
test.test_float
test.test_heapq
test.test_bisect
test.test_operator
test.test_fractions
# test_datetime runs the FULL CPython 3.14 datetimetester (vendored, only
# GemStone-forced # GRAIL: adaptations). The former 3-way split existed only
# because whole-module codegen was O(output^2); fixed (PrettyWriteStream O(1)),
# so the ~7300-line combined file now transpiles in ~0.3s.
test.test_datetime
test.test_re
test.test_functools

# --- Phase 2 (enabled 2026-07-11: MI/C3, closure cells, int subclassing,
#     unittest subTest/assertWarns, and the catchable-error batches have
#     landed; sibling mixins vendored) ---
test.test_list
test.test_tuple
test.test_dict
test.test_set
test.test_bytes
test.test_collections
test.test_itertools
test.test_enum

# --- Phase 3 (added 2026-07-30: easier P1 "core language & built-in types"
#     modules from docs/Grail_CPython_Scope.md.  Selected by trialing the P1
#     tier and keeping those that import cleanly and score OK / low-error;
#     harder P1 modules (test_isinstance/index/listcomps errors,
#     test_enumerate/global IMPORTERROR, test_genexps doctest-only) are
#     deferred until the underlying codegen/builtin gaps close.) ---
test.test_unary
test.test_int_literal
test.test_unpack
test.test_augassign
test.test_contains
test.test_dictcomps
test.test_setcomps
test.test_pow
test.test_richcmp
test.test_slice
test.test_bool
# test_iter: builtins>>iter: now honours the __iter__ = None sentinel (was a
# CRASH -- iter() eagerly materialised an unbounded __getitem__ into a VM OOM);
# the two remaining iter(UnlimitedSequenceClass()) OOM tests are skip-listed
# (scripts/cpython_suite_skips.txt) pending a lazy __getitem__ seqiterator.
test.test_iter
#
# test_traceback: the traceback / PEP 657 conformance module
# (docs/Python_Traceback_Design.md).  Added as a tracked BASELINE -- it does not
# import yet.  Vendoring it pulled in linecache.py + a _colorize.py stub + several
# test.support / os_helper / import_helper symbols; the remaining import blocker is
# ``__code__'' on a class/module-level def (a BoundMethod), which Phase 2a
# explicitly deferred (only nested-def ExecBlocks carry __code__ today).  The
# scoreboard's detail column names the current blocker; grow from there.
test.test_traceback

# --- Phase 4 (added 2026-08-03) ---
#
# Selected the same way as Phase 3, but from a WIDER trial: all 55 remaining
# P1/P2 candidates in docs/Grail_CPython_Scope.md were vendored and scored once
# (446s at 5 workers), then kept or dropped on the evidence.  Kept here are the
# modules that RUN today and whose residual is either small (an easy win) or
# traces to ONE root that also blocks other modules; every module that was
# trialed and dropped is listed -- with the blocker its log named -- in
# docs/Grail_CPython_Scope.md ("Trialed and deferred"), so nobody has to
# re-trial to find out why it is absent.
#
# Easy wins -- small residual, no new runtime plumbing:
test.test_compare
test.test_iterlen
test.test_keywordonlyarg
test.test_dictviews
test.test_sort
test.test_generator_stop
test.test_userdict
test.test_userlist
test.test_isinstance
#
# Single-root modules: most of the failures in each collapse to one gap, and
# that gap leaks into modules already on the board (an uncatchable Smalltalk
# DNU reaching Python code, or a codegen nil receiver):
#   test_index         32/34 errors are `a newstyle does not understand #'<'/#'<='''
#   test_baseexception all 7 errors are #signal / #handles: sent to a non-exception
#   test_listcomps     29/52 errors are `UndefinedObject does not understand #new''
#   test_property      12/23 failures are property.__doc__ falling back to object's
#   test_copy          copy.Error is missing; deepcopy returns identical objects
test.test_index
test.test_baseexception
test.test_listcomps
test.test_property
test.test_copy
#
# Core language / container surface, moderate residual but high leverage
# (function attributes + LEGB, generator delegation, deque, %-format):
test.test_scope
test.test_yield_from
test.test_deque
test.test_format
#
# Two of the new modules each contain ONE test that HANGS the scoring session
# (unbounded work, uncatchable): test_deque.test_extend and
# test_format.test_common_format are skip-listed in
# scripts/cpython_suite_skips.txt, which names the bug in each case -- removing
# the skip is the regression test for the fix.
#
# test_range is NOT here yet, and is the one deliberate omission from the trial
# set that scored: range's count/index/__contains__/len walk the range instead of
# computing arithmetically (CPython's are O(1) for int arguments), so EVERY
# 10**20-scale test in the module hangs the session -- test_count,
# test_iterator_unpickle_compat (range_iterator has no __setstate__, so the saved
# index is dropped and the iterator restarts at -2**65) and test_large_range, at
# least, found one after the other by skip-and-rerun.  Chasing them with skips
# costs a 600s watchdog cycle each and would leave the module mostly skipped;
# land the arithmetic-range fix first, then add the module in one step.
#
# NOTE: test_json and test_string are PACKAGES (directories) in 3.14, not
# single modules -- out of scope for the V1 single-module harness.
#
# Not wired, and not for lack of trying: test_metaclass, test_unpack_ex,
# test_genexps, test_descrtut, test_extcall and test_pep646_syntax carry NO
# unittest.TestCase at all -- they are pure doctest modules, which
# _grail_harness's discovery cannot see, so each scores SKIP with 0 tests and
# reads on the board like a module that passed nothing rather than one nobody
# can measure.  8 of the 179 unwired-but-importable in-scope modules are this
# shape (scripts/cpython_import_census.py --cpython-lib counts them).  Teaching
# the harness doctest discovery is what unlocks them.
test.test_binop
test.test_complex
test.test_enumerate
test.test_raise
test.test_funcattrs
test.test_decorators
test.test_print
#
# Nor test_class: line 859 is a bare top-level ``from _testinternalcapi import
# has_inline_values'', so the module cannot import at all, and
# _testinternalcapi is CPython interpreter internals -- explicitly
# out-of-scope in docs/Grail_CPython_Scope.md.  Wiring it would park a
# permanently red row whose blocker nobody intends to fix.  The other 858
# lines are ordinary class-model tests; they become reachable if that import
# is ever stubbed.
test.test_builtin
test.test_call
test.test_dynamic
test.test_exception_variations
test.test_global
test.test_hash
test.test_named_expressions
test.test_subclassinit
test.test_super
test.test_typechecks
test.test_with
test.test_positional_only_arg
test.test_string_literals
test.test_genericclass

# --- P3 probe batch: a SYSTEMATIC SAMPLE, not a curated one -----------------
# Every earlier batch was chosen -- areas Grail already covered, modules that
# looked close.  That is fine for making progress and useless for forecasting,
# because a hand-picked cohort tells you nothing about the ones nobody picked.
# P3 and P4 are 131 of the 255 in-scope modules and exactly ONE of them had
# ever been measured, so half the scope was being extrapolated from zero data.
#
# So: of the 47 P3 modules whose subject imports today (scripts/
# cpython_import_census.py), sort by name and take every 4th.  No skipping the
# awkward ones -- test_typing is 11k lines and test_codecs 3.9k, and they are
# in precisely because a sample that drops them measures nothing.  Whatever
# these score IS the P3 estimate.
#
# Vendored from CPython 3.14.6; the rest of the corpus is 3.14.4.  A patch
# release apart, but it is the reason a vendored file may differ slightly from
# what the header above claims.
test.test_annotationlib
test.test_bufio
test.test_codecs
test.test_contextlib_async
test.test_asyncgen
test.test_coroutines
test.test_difflib
test.test_gettext
test.test_linecache
test.test_pickle
test.test_reprlib
test.test_struct
test.test_typing
test.test_warnings

# --- P4 probe batch: the same SYSTEMATIC SAMPLE, applied to the last tier ----
# P4 is 75 modules, 29% of the in-scope corpus, and NONE had ever been
# measured -- the one part of the "how far is this from done" estimate still
# resting on zero data.  The P3 batch above closed the other half; this closes
# this one.
#
# Of the 69 P4 modules CPython ships as a single test_*.py, sort by name and
# take every 6th.  No skipping the awkward ones: test_ssl is 5.7k lines and
# test_urllib2_localnet wants a live server, and they are in for exactly the
# reason test_typing was -- a sample that drops what looks hard measures
# nothing.  Whatever these score IS the P4 estimate.
#
# ONE DELIBERATE DIFFERENCE from the P3 batch, which matters when comparing
# the two numbers: P3 was drawn only from modules whose SUBJECT already
# imports.  This one is not filtered that way, because for P4 the open
# question is not "how many nearly pass" but "how much of it is reachable at
# all" -- and pre-filtering on the subject importing would answer that by
# assuming it.  Expect a lower measurable rate than P3's for that reason
# alone; the two rates are not like-for-like.
#
# Vendored from CPython 3.14.6, as the P3 batch was.
#
# RESULT: 1 of 12 measurable -- test_htmlparser, 67 tests, 47 passing on first
# contact.  The other 11 scored IMPORTERROR, and WHY matters more than the
# count: only 5 were blocked on a subject Grail does not have (netrc, wave,
# xml.sax x2, zipapp).  The other 6 died on an unvendored TEST-SUPPORT HELPER
# -- socket_helper, hashlib_helper, multibytecodec_support, check_sanitizer,
# and support.LARGEST (added in this batch) -- which says nothing about Grail
# at all.
#
# That is the batch's real finding, and it is a correction to
# cpython_import_census.py rather than to the P4 estimate: the census probes
# the SUBJECT import and cannot see helper imports, so it would have called 7
# of these 12 "ready to wire" when 1 scored.  Recorded in that script's
# docstring.  Expect blockers to STACK behind each helper: clearing LARGEST
# advanced test_ipaddress to needing IPv6Address, which Grail's IPv4-only
# ipaddress does not have.
test.test___all__
test.test_codecencodings_kr
test.test_codecmaps_tw
test.test_htmlparser
test.test_ipaddress
test.test_netrc
test.test_pulldom
test.test_sax
test.test_ssl
test.test_urllib2_localnet
test.test_wave
test.test_zipapp

# --- test_asyncio, one SUBMODULE at a time --------------------------------
#
# CPython's test_asyncio is a 41-file, 31k-line PACKAGE, and naming the package
# here would score 0 tests: the driver discovers unittest.TestCase subclasses
# defined in ONE named module (test._grail_harness >> score), and the package's
# __init__.py defines none.  Naming submodules works instead -- the driver
# resolves any dotted name through importlib ___moduleNameToPath___ -- which is
# what makes this corpus adoptable incrementally rather than all at once.
#
# The split that decides the order is test_asyncio/utils.py.  22 of the 38 test
# files import it, and it needs selectors, socketserver, threading,
# unittest.mock, http.server, wsgiref.simple_server, and three asyncio
# submodules Grail does not have (base_events, format_helpers, log).  The other
# 16 need only unittest.IsolatedAsyncioTestCase, which is a 158-line port --
# and that is ~4,950 lines of upstream tests for the primitives that are next
# anyway (locks 1825, taskgroups 1118, queues 725, timeouts 411, waitfor 353).
#
# test_context is first because it is the smallest (38 lines) and needs
# NEITHER: plain unittest.TestCase, and only gather/sleep/run from asyncio. It
# is here to prove the mechanism end to end, and it is expected to SKIP --
# decimal.HAVE_CONTEXTVAR is False because Grail's decimal shares one module
# global, so upstream's skipUnless is doing its job rather than being dodged.
test.test_asyncio.test_context
